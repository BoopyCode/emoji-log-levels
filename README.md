# Emoji Logger

Turns boring log levels (INFO, ERROR, DEBUG) into expressive emojis with colors. Makes debugging less soul-crushing.

Usage: `python emoji_logger.py your_log_file.log` or pipe logs: `tail -f app.log | python emoji_logger.py`